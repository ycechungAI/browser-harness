# Claude Code instructions

Read and follow `AGENTS.md`.

For browser work in this checkout, use `./browser-harness` so the current
source and its default recorder are active.

For any browser-harness recording or video task, read
`skills/browser-harness-video/SKILL.md` fully before editing. Reuse the shared
renderer and run its blocking review; do not invent a separate video workflow.
